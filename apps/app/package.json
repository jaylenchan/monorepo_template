{
  "name": "jeditor-app",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "dev": "yarn build && electron ./dist/main.js",
    "run": "electron ./dist/main.js"
  },
  "devDependencies": {
    "electron": "^20.1.0",
    "tsup": "^6.7.0"
  },
  "dependencies": {
    "@jeditor/transpiler": "workspace:^"
  }
}
