{
  "name": "jeditor-app",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "compile": "npx tsc -b",
    "dev": "electron ./dist/main.js"
  },
  "devDependencies": {
    "electron": "^25.1.0"
  }
}
